source: https://github.com/Joshuar288/Vetec-Docente-Innovador

nginx:
  root: public_html/dist
  index: index.html
  locations:
    - match: /
      try_files: $uri $uri/ /index.html

commands:
  - test -f dist/index.html
